import siteSettings from "./siteSettings";
import { heroSchema, categoriaSchema, subcategoriaSchema, productoSchema } from "./schemas";

export const schemaTypes = [
  siteSettings,
  heroSchema,
  categoriaSchema,
  subcategoriaSchema,
  productoSchema,
];
